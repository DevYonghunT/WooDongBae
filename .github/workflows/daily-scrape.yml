name: Daily Course Scraper

on:
  # 1. 자동 실행 스케줄 (UTC 기준)
  # 한국 시간 오전 4시 = UTC 19:00 (전날)
  schedule:
    - cron: '0 19 * * *'

  # 2. 수동 실행 (GitHub 웹에서 버튼 클릭)
  workflow_dispatch:
    inputs:
      target:
        description: '특정 지역/기관만 실행하려면 입력 (예: 성남시)'
        required: false
        default: ''

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    
    # 시간대 설정 (로그 시간 확인용)
    env:
      TZ: 'Asia/Seoul'

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🟢 Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 의존성 패키지 설치 (dev 포함)
        run: npm ci --include=dev

      - name: 🎭 Playwright 브라우저 설치
        run: npx playwright install --with-deps chromium

      - name: 🔐 환경 변수 파일 생성 (.env.local)
        run: |
          echo "NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" >> .env.local
          echo "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" >> .env.local
          echo "NEXT_PUBLIC_API_KEY=${{ secrets.NEXT_PUBLIC_API_KEY }}" >> .env.local
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env.local
          
      - name: 🚀 스크래퍼 실행 (전체 또는 타겟)
        run: |
          if [ -n "${{ github.event.inputs.target }}" ]; then
            echo "🎯 타겟 지정 모드: ${{ github.event.inputs.target }}"
            npx ts-node --esmx scrapers/main.ts --target="${{ github.event.inputs.target }}"
          else
            echo "🔄 전체 모드 실행"
            npx ts-node --esm scrapers/main.ts
          fi
